spring:
  datasource:
    url: jdbc:h2:mem:wareflow;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  kafka:
    bootstrap-servers: localhost:9092
wareflow:
  seed:
    enabled: false
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
